{
  "name": "@mathhack/site",
  "version": "0.1.0",
  "private": true,
  "description": "MathHack download site",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "preview": "vite preview"
  }
}
